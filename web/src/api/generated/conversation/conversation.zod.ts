/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Firetower
 * The Firetower control plane: API, scheduling, and worker transports.
 * OpenAPI spec version: 0.11.0
 */
import * as zod from 'zod';


/**
 * Two different things arrive through one channel, because both are tool calls
 * that fall through to the permission prompt. They are not the same question:
 * one asks whether something may happen, and the other asks which of several
 * things should. Drawn identically, the second becomes a card offering
 * "allow" and "deny" to a question about output format.
 * The pickers this session offers.
 *
 * Per session rather than a constant in the browser, because which knobs a
 * session has is a fact about the agent it runs — and it was three constants
 * in a React file for as long as there was one agent to be right about.
 * @summary What the agent has stopped for, as the interface should draw it.
 */
export const SessionControlsParams = zod.object({
  "id": zod.string().describe('Session id')
})

export const SessionControlsResponseItem = zod.object({
  "choices": zod.array(zod.object({
  "grave": zod.boolean().optional().describe('Drawn apart, because it changes what the agent may do unsupervised.'),
  "label": zod.string().describe('What the picker shows when this is in force.'),
  "note": zod.string().nullish().describe('Why somebody would pick it, when that is not obvious.'),
  "value": zod.string().describe('What gets sent.')
}).describe('One option in a picker.')),
  "current": zod.string().nullish().describe('What is in force, when the agent has said.'),
  "fallback": zod.string().describe('Shown when nothing is in force yet.'),
  "kind": zod.enum(['model', 'mode', 'effort', 'sandbox']).describe('Which setting a picker changes.')
}).describe('One picker: what it changes, what it offers, and what is in force.')
export const SessionControlsResponse = zod.array(SessionControlsResponseItem)

/**
 * @summary Change one of them.
 */
export const ChooseControlParams = zod.object({
  "id": zod.string().describe('Session id')
})

export const ChooseControlBody = zod.object({
  "kind": zod.enum(['model', 'mode', 'effort', 'sandbox']).describe('Which setting a picker changes.'),
  "value": zod.string()
}).describe('What to change, and to what.')

export const ChooseControlResponse = zod.object({
  "sent": zod.boolean()
})

