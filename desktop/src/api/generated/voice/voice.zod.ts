/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Firetower
 * The Firetower control plane: API, scheduling, and worker transports.
 * OpenAPI spec version: 0.38.1
 */
import * as zod from 'zod';


/**
 * [`vault::Vault::holds`] rather than a read: this is a screen asking whether
 * a button should work, and it decrypts nothing and writes no log line. A
 * microphone drawn on every composer must not cost an audit entry each time
 * one opens.
 * @summary Whether dictation can be used here, and whether you can fix it if not.
 */
export const VoiceStateResponse = zod.object({
  "configured": zod.boolean().describe('A key is held. Not whether it works — that is only known by using it.'),
  "mayConfigure": zod.boolean().describe('Whether this person may set or replace it. Administrators only.')
}).describe('What dictation knows about itself before anybody presses anything.')

/**
 * @summary Hold a key for this install, or replace the one held.
 */
export const SetVoiceKeyBody = zod.object({
  "key": zod.string().describe('The OpenAI key. Replaces whatever is there; the old one is not recoverable.')
})

export const SetVoiceKeyResponse = zod.void()

/**
 * @summary Mint one, for one connection.
 */
export const VoiceTicketResponse = zod.object({
  "expiresAt": zod.string().describe('When it stops being usable, RFC 3339. Only the connection has to happen\nbefore this; the session it opens outlives it.'),
  "value": zod.string().describe('The ephemeral client secret, `ek_…`. Not the key, and not a secret this\ninstall stores: it is minted, handed over and forgotten.')
}).describe('A credential for one connection, with about a minute to use it.')

