{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "island",
  "description": "What the island is allowed to do to itself. Its own capability rather than a second label on `default`: the island may drag itself and listen, and it may do nothing else. Every other thing it needs — where the displays are, where to sit, whether to be visible — goes through a command in `island.rs`, and a command is not a permission, so the list stays this short on purpose.",
  "windows": [
    "island"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:event:allow-emit"
  ]
}
